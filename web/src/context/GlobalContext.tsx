import React from 'react';

export type ContextProps = {
    children: React.ReactNode
}

const GlobalContext = () => {
    return (
        <div>
            
        </div>
    );
};

export default GlobalContext;