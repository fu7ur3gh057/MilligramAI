import styled from "styled-components";
import {Link} from "react-router-dom";


interface ButtonProps {
    border?: boolean
    bgColor?: string,
    borderRadius?: string,
    margin?: string,
    padding?: string,
    gap?: string
}
